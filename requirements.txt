anthropic>=0.40.0
flask>=3.0.0
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.0
SpeechRecognition>=3.10.0
edge-tts>=7.0.0
playwright>=1.40.0
faster-whisper>=1.0.0
imageio-ffmpeg>=0.4.9
# Pillow/numpy: für den Website-Werbevideo-Reiter (Screenshot-Zuschnitte, Text-Karten,
# Kontrast-Heuristik) — unabhängig vom optionalen torch/diffusers-Media-Stack nötig.
Pillow>=10.0.0
numpy>=1.24.0
pyttsx3>=2.90
waitress>=3.0.0
discord.py>=2.4.0
beautifulsoup4>=4.12.0
# ── LeadForge / leadpackages (Datenpakete-Modul) ──────────────────────────────
scrapling>=0.4.9
# scrapling.fetchers.Fetcher braucht curl_cffi (Browser-Impersonation) — scrapling zieht
# es nicht auf jeder Plattform automatisch als Pflicht-Dependency (beobachtet: fehlte auf
# einer Zweit-Maschine, obwohl requirements.txt frisch installiert war -> herold.at-Worker
# schlug bei JEDER Anfrage mit ModuleNotFoundError fehl). Explizit pinnen, damit es garantiert
# mitkommt statt sich auf scrapling's eigene Extras zu verlassen.
curl_cffi>=0.7.0
rapidfuzz>=3.14.0
openpyxl>=3.1.0
# Optional (nur fuer die MCP-Bruecke der lokalen KIs): pip install mcp ollama
# Optional (echte MX-Record-Pruefung in email_verify.py): pip install dnspython
#   -> ohne dnspython faellt die Zustellbarkeits-Pruefung sauber auf socket.getaddrinfo
#      (A-Record) zurueck; dnspython erkennt zusaetzlich Domains mit A- aber ohne MX-Record.
